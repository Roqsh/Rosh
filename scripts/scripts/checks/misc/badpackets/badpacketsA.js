import config from "../../../data/config.js";
import { flag } from "../../../util";

/**
 * @name badpackets_a
 * @param {player} player - The player to check
 * @remarks Checks for 
*/

export function badpackets_a(player) {

    if(config.modules.badpacketsA.enabled) {
        return;
    } 
}
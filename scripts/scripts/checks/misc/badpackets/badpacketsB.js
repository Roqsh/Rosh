import config from "../../../data/config.js";
import { flag } from "../../../util";

/**
 * @name badpackets_b
 * @param {player} player - The player to check
 * @remarks Checks for 
*/

export function badpackets_b(player) {

    if(config.modules.badpacketsB.enabled) {
        return;
    } 
}